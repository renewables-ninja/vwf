
	# our wind farms file with coordinates, turbine models, etc.
	windFarmFile = 'Q:/VWF/data/INPUT_CH.CSV'

	# the simplified column names used in these files (must be the same across all files)
	windFarmCols = c('id', 'iso', 'name', 'lon', 'lat', 'offshore', 'height', 'count', 'capacity', 'power_curve', 'PR')
